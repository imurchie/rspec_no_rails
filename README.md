Basic test using Sauce Ruby gem but not in any kind of framework (Rails, etc.).

Work it:

```shell
bundle install
bundle exec rspec
```

Need to add `parallel_gem` access so jobs aren't only run serially.
